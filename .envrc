if ! command -v nix_direnv_version >/dev/null 2>&1; then
  echo "gh-loupe: nix-direnv 3.1 or newer is required for direnv; use 'nix develop' instead" >&2
  return 1
fi

nix_direnv_version 3.1
use flake .
